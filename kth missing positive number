class Solution {
    public int findKthPositive(int[] arr, int k) {
      int l=0;
      int h=arr.length-1;
      while(l<=h){
        int mid=(l+h)/2;
        int correctno=mid+1;
        int missingno=arr[mid]-correctno;
        if(missingno>=k) h=mid-1;
        else l=miad+1;
      }  
      return  h=k+l;
    }
}
